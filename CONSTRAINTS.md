# Constraints (this repo)

Clearing is a new instance. Not `xray/`. Not a mill plant. Not usmail.

- **Wear 0xray.** `npm i 0xray` in this tree when we start code. Garment only.
- **Not inside the 0xray tarball.** Do not vendor Clearing into `0xray`. Do not mill-plant. Do not `npx @0xray/foundry mint`.
- **No eighth 0xray MCP.** The seven servers stay `xray-*`. Repertoire is an extra host MCP, not `xray-*`. Clearing is a **product** MCP named `clearing`, never `xray-clearing`.
- **Keys stay on the rail.** No wallet in this repo. No key in model context.
- **Do not `railway link` this machine’s usmail-ai project to Clearing.** If extract HTTP needs a host, create a new Railway project. Never attach `usmail-ai`.
- **Mill stays mill.** `0xray@4.0.6` PUT is a different job. This tree does not carry mill flesh.
- **Card is this repo’s** `.xray/state/STATION.md`. Do not continue a mill or xray card here.

`public/agents.md` is the product distribution card (HTTP `/agents.md`). Root `AGENTS.md` is the 0xray suit. They collide on macOS case-insensitive disks — never store the product card at repo-root `agents.md`.

`.mcp.json` has seven `xray-*` servers plus optional `repertoire` plus product `clearing`. Never `xray-clearing`.
